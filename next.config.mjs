/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['w0.peakpx.com', 'www.instagram.com','encrypted-tbn0.gstatic.com'],
    },
  }
  
  export default nextConfig;
  